## Backtest service environment
#
# The backtest service runs separately from the Node API. For intraday strategies
# (like Opening Range), it needs its own access to FMP.
#
# You can either:
# - copy your key from `server/.env`, or
# - set it in your shell before starting `npm run dev:backtest`.
#
FMP_API_KEY=

